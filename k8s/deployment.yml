apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-iris-app
  labels:
    app: flask-iris
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-iris
  template:
    metadata:
      labels:
        app: flask-iris
    spec:
      containers:
      - name: flask-iris
        image: arunbits/test:latest
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: flask-iris-service
spec:
  type: LoadBalancer
  selector:
    app: flask-iris
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
